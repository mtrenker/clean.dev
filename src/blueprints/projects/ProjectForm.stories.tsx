import React, { FC } from 'react';

import { ProjectForm } from './ProjectForm';

export default ({ title: 'Blueprints | Projects/ProjectForm' });

export const Component: FC = () => (<ProjectForm />);
