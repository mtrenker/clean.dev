import React, { FC } from 'react';

import { Select } from './Select';
import { Option } from './Option';

export default ({ title: 'Components | Form/Select' });

export const Normal: FC = () => (<Select><Option>Test</Option></Select>);
