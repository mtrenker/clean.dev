import React, { FC } from 'react';
import { ProjectEditor as Component } from './ProjectEditor';

export default ({ title: 'Components/Blueprints/ProjectEditor' });

export const ProjectEditor: FC = () => (<Component />);
