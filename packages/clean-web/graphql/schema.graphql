scalar AWSDate

schema {
  query: Query
  mutation: Mutation
}

type Contact {
  city: String
  company: String
  country: String
  email: String
  firstName: String
  lastName: String
  street: String
  zip: String
}

type Mutation {
  contact(email: String, message: String, name: String): String
  createProject(contact: ContactInput, project: ProjectInput!): Project!
  removeProject(id: ID!): String!
  updateProject(contact: ContactInput, id: ID!, project: ProjectInput!): Project!
}

type Project {
  client: String!
  contact: Contact
  endDate: AWSDate
  featured: Boolean
  highlights: [String]
  id: ID!
  location: String
  position: String!
  startDate: AWSDate
  summary: String!
}

type Query {
  projects: [Project!]!
}

input ContactInput {
  client: String!
  endDate: AWSDate
  featured: Boolean
  hightlights: [String]
  location: String
  position: String!
  startDate: AWSDate
  summary: String!
}

input ProjectInput {
  client: String!
  endDate: AWSDate
  featured: Boolean
  hightlights: [String]
  location: String
  position: String!
  startDate: AWSDate
  summary: String!
}
